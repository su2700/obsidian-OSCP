
```
sudo sh -c 'echo "10.10.10.161 FOREST.htb.local" >> /etc/hosts'
```

```
echo "$ip domain" | sudo tee -a /etc/hosts

```

```
echo "192.168.191.97 dc01.secura.yzx dc01" | sudo tee -a /etc/hosts
```